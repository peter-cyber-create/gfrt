# Optional reverse-proxy example for GFRT production.
# Operator-managed: do NOT apply automatically. Copy/adapt on the host Nginx.
# Upstream containers expose api:4000 and web:80 on the private Docker network
# (or bind to 127.0.0.1 via a local publish if you choose).

# upstream gfrt_web { server 127.0.0.1:8080; }
# upstream gfrt_api { server 127.0.0.1:4000; }
#
# server {
#   listen 443 ssl http2;
#   server_name app.example.org;
#   # ssl_certificate     /etc/ssl/...;
#   # ssl_certificate_key /etc/ssl/...;
#
#   location /api/ {
#     proxy_pass http://gfrt_api;
#     proxy_set_header Host $host;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   }
#   location /health { proxy_pass http://gfrt_api/health; }
#   location /ready  { proxy_pass http://gfrt_api/ready; }
#   location / {
#     proxy_pass http://gfrt_web;
#     proxy_set_header Host $host;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
