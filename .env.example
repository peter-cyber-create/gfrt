# Root recon tooling only. Never commit real secrets.
MUSOOKA_BASE_URL=https://musooka.site
# Presentation/recon credentials only — do NOT seed into the new production database.
MUSOOKA_EMAIL=
MUSOOKA_PASSWORD=
