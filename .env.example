# Root recon tooling only. Never commit real secrets.
# Do not point GFRT production at musooka.site.
MUSOOKA_BASE_URL=
MUSOOKA_EMAIL=
MUSOOKA_PASSWORD=

# Frontend env lives in clone/.env.example (GitHub Pages demo defaults to VITE_DEMO_MODE=true).
# See docs/GITHUB_PAGES.md
